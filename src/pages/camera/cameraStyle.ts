import styled from 'styled-components/native'
import Constants from 'expo-constants'
import { calcRem } from '../../utils/mixins'
import { primary } from '../../styles/Colors'

export const CameraBody = styled.View`
  height: 100%;
  width: 100%;
  background: black;
`